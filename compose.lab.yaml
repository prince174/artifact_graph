# Optional, privileged integration-test agent. Never combine with compose.prod.yaml.
# docker compose -f compose.yaml -f compose.lab.yaml --profile build-lab up -d --build teamcity-agent
services:
  teamcity-agent:
    image: artifact-graph-lab-agent:local
    build:
      context: .
      dockerfile: config/lab-agent/Dockerfile
